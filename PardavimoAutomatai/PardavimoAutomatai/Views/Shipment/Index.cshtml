@using PardavimoAutomatai.Models;
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        @*public int id { get; set; }
        public string start { get; set; }
        public string comfirm { get; set; }
        public string done { get; set; }
        public int admin { get; set; }
        public int vendor { get; set; }
        public int supplier { get; set; }*@
        <th>ID</th>
        <th>Start date</th>
        <th>Comfirm date</th>
        <th>End date</th>
        <th>Done</th>
        <th>Admin</th>
        <th>Vendor</th>
        <th>Supplier</th>
        <th></th>
    </tr>

    @foreach (var item in (List<Shipment>)Session["List"])
    {
    <tr>
        <td>
            @item.id
        </td>
        <td>
            @item.start
        </td>
        <td>
            @item.comfirm
        </td>
        <td>
            @item.end
        </td>
        <td>
            @item.done
        </td>
        <td>
            @item.admin
        </td>
        <td>
            @item.vendor
        </td>
        <td>
            @item.supplier
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
            @Html.ActionLink("Details", "Details", new { id = item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.id })
        </td>
    </tr>
    }

</table>
